import styled from "@emotion/styled";

const Headline = styled.h1`
  color: #FFF;
  font-family: 'Inter', sans-serif;
  font-size: 36px;
  font-style: normal;
  font-weight: 500;
  line-height: 38px;
  padding: 0;
  margin: 0;
  transform: scaleX(0.5);
  transform-origin: left center;
`;

const Title = styled.h2`
  color: #FFF;
  font-family: 'Inter', sans-serif;
  font-size: 24px;
  font-style: normal;
  font-weight: 400;
  line-height: 25px;
  padding: 0;
  margin: 0;
  display: inline-block;
  transform: scaleX(0.5);
  transform-origin: left center;
`;

const Body = styled.p`
  color: #FFF;
  font-family: 'Inter', sans-serif;
  font-size: 12px;
  font-style: normal;
  font-weight: 400;
  line-height: 14px;
  padding: 0;
  margin: 0;
`;

const SupportingText = styled.p`
  color: #FFF;
  font-family: 'Inter', sans-serif;
  font-size: 10px;
  font-style: normal;
  font-weight: 400;
  line-height: 12px;
  padding: 0;
  margin: 0;
`;

export { Headline, Title, Body, SupportingText };
